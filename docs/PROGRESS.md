# PROGRESS
- [r001] (b9e3013) : 2026.04.26 完成 Zembra Rust 后端基础设施初始化，记录技术选型结论并建立 docs/request-clarify/r001-backend-tech-stack.md、docs/design-docs/r001-backend-tech-stack.md 和 docs/exec-plans/active/r001-backend-tech-stack.md，创建 Rust 2024 Cargo 工程、Axum 服务入口、配置样例、模块骨架与 /health 健康检查，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证。
- [r002] (8e4b0b0) : 2026.04.26 完成用户配置文件读取功能，统一配置字段为 database.path，支持读取 ~/.zembra.env 覆盖默认配置并在缺失时输出 warning，运行时自动转换为 SQLx SQLite URL，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证，等待用户验收。
- [r003] (930900a) : 2026.04.27 完成共享数据库 schema submodule 引入，新增 vendor/zembra-schema 并固定到 v0.2.0/cd37a7e，补充 r003 需求澄清、设计文档和执行计划，将 .env.example 对齐为 TOML 配置样例，已通过 schema tag 校验、cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证，等待用户验收。
- [r004] (e3d28f5) : 2026.05.03 完成 Server CRUD API 落地与收尾记录，建立 docs/design-docs/r004-server-crud-api.md 和 docs/exec-plans/active/r004-server-crud-api.md，实现 SQLite v0.2.0 migration、连接池、Repository、Service、HTTP routes 与统一错误响应，补充 docs/http-client-server-api.md 并将 .DS_Store 加入 .gitignore，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证。
- [r005] (working tree) : 2026.05.03 修复 `~/.zembra.env` 用户配置加载逻辑，保留 `.env` 文件名并显式按 TOML 格式解析；同时兼容已有 v0.2.0 SQLite 表但缺失 `schema_migrations` 的本地库，启动时补建迁移记录而不重复建表。已通过 cargo fmt --check、cargo test config、cargo test database、cargo check 和 cargo test 验证，待正常启动服务复测。
- [r006] (7cb813d7) : 2026.05.03 完成 Recent Notes API 游标翻页扩展，更新 docs/request-clarify/r006-recent-notes-api.md、docs/design-docs/r006-recent-notes-api.md 和 docs/exec-plans/active/r006-recent-notes-api.md，`POST /notes/recent` 支持完整 32 位 hex `note_uuid` 游标，按 `(updated_at, id)` 稳定返回更旧笔记并过滤删除和归档内容，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证。
- [r007] (0515d41) : 2026.05.04 完成日志模块开发，新增 `logging.level` 和 `logging.path` 配置，默认显示级别 `INFO`、默认目录 `logs`，日志按天写入文件；服务启动成功后打印数据库路径、base url 和 OpenAPI API 列表，已通过 cargo fmt --check、cargo check、cargo test、cargo clippy 和本地服务启动验证，等待用户验收。
- [r008] (working tree) : 2026.05.04 将数据库 schema 升级到 v0.3.0，更新 `vendor/zembra-schema` submodule 指针，接入 `003_add_bidirectional_sync.sql`，现有 notes/taxonomy API 统一使用默认 workspace 兼容新 schema；已通过 cargo fmt --check、cargo check、cargo test、cargo clippy、/health、OpenAPI JSON 和本地数据库 schema_migrations 验证，服务已启动等待用户验收。
- [r009] (working tree) : 2026.05.04 完成 Supabase 后台同步接入第一版，新增 `.zembra.env` sync 配置、Supabase Postgres migration、本地 `sync_changes` 生成、Supabase REST client、push/pull service、后台 worker 和 `/sync/status`、`/sync/run`、`/sync/push`、`/sync/pull` API；已通过 cargo fmt --check、cargo check、cargo test、cargo clippy -- -D warnings、/health 和 OpenAPI JSON 验证，等待用户验收。
- [r010] (working tree) : 2026.05.10 完成 Supabase 同步配置 API，新增配置读写服务、运行时 sync 配置热更新、`GET /sync/config`、`PUT /sync/config`、`POST /sync/config/test` 和 OpenAPI 注册；配置写回 `~/.zembra.env`，响应不返回 service role key 明文，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy -- -D warnings 验证，等待用户验收。
- [r023] (working tree) : 2026.05.28 完成 Supabase 新版 secret key 接入改造，配置/API 字段统一为 `secret_key` 和 `secret_key_configured`，运行时只接受 `sb_secret_...`，旧版 legacy service role key 会被拒绝；已通过 cargo fmt --check、cargo test config、cargo test、cargo check 和 cargo clippy -- -D warnings 验证，等待用户验收。
- [r025] (working tree) : 2026.05.31 将共享数据库 schema 升级到 v0.4.0，更新 `vendor/zembra-schema` submodule 指针，接入 `004_add_hierarchical_tags.sql`，新建标签按 `parent_tag_id`、`path`、`depth` 写入层级节点，现有 notes/taxonomy API 继续通过完整路径返回标签；已通过 schema tag 校验、cargo fmt --check、cargo check、cargo test 和 cargo clippy -- -D warnings 验证，等待用户验收。
- [r026] (working tree) : 2026.05.31 完成 v0.4.0 层级标签代码适配，`TagRecord` 暴露 `parent_tag_id`、`path`、`depth` 并让 `name` 回归当前节点名；`/tags`、note tags、random tags 返回结构化 tag，metadata tags 保持完整 path 字符串；sync tag payload 要求 `path/depth` 并补充结构化 apply/conflict 测试。已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy -- -D warnings 验证，等待用户验收。
- [r011] (working tree) : 2026.05.15 完成局域网网页访问后端服务支持，`server.host` 改为标准 IPv4 字符串配置，新增 `server.cors_allowed_origins` 和 Axum CORS layer，补充普通 Linux 主机局域网部署说明；已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy -- -D warnings 验证，等待用户验收。
- [r011-fix] (working tree) : 2026.05.15 修正 CORS 默认策略，空配置下默认只允许 localhost 和 loopback origin，解决 `cargo run` 后本机前端默认无法跨域访问后端的问题；局域网其他机器、公网或自定义域名 origin 仍需通过 `server.cors_allowed_origins` 显式配置。
- [r011-wildcard] (4f91f23) : 2026.05.16 完成 CORS 局域网 IPv4 通配符配置，支持 `http://192.168.1.*:5173` 这类 IPv4 octet 通配并保持端口精确匹配，拒绝域名通配符和端口通配符；已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy -- -D warnings 验证，执行计划为 docs/exec-plans/active/r011-lan-web-access.md。
- [r013] (fc3f623) : 2026.05.16 完成 Random Tags API 及 count 参数扩展，`GET /random/tags?n=N&count=CNT` 支持随机 tag 分组、累计 notes 上限、软删除和归档过滤，并同步 OpenAPI、自动化测试、需求澄清和设计文档；执行计划已归档为 docs/exec-plans/completed/r013-random-tags-api.md，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证。
- [r014] (fc3f623) : 2026.05.16 完成 Random Fields API，`GET /random/fields?n=N&count=CNT` 支持随机 field 分组、累计 notes 上限、空 field 保留、软删除和归档过滤，并同步 OpenAPI、自动化测试、需求澄清和设计文档；执行计划已归档为 docs/exec-plans/completed/r014-random-fields-api.md，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证。
- [r015] (fc3f623) : 2026.05.16 完成 Random Notes API，`GET /random/notes?n=N` 支持必填数量校验、随机返回 1 到 50 条未删除未归档笔记、笔记不足时返回现有数量、无可用笔记时返回空数组，并同步 OpenAPI、自动化测试、需求澄清和设计文档；执行计划已归档为 docs/exec-plans/completed/r015-random-notes-api.md，已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy 验证。
- [r016] (5cb6ff47) : 2026.05.21 完成 POST Note Edit API 需求调研并确认不实施新入口，当前后端已有 `PATCH /notes/{note_ref}` 编辑能力，支持完整 ID 与唯一短前缀并写入 revision 和 sync changes；本次决策保留现有 PATCH 合同，将澄清文档归档到 docs/request-clarify/deprecated/r016-post-note-edit-api.md。
- [r017] (5195760f) : 2026.05.21 完成 PATCH Note Taxonomy API 扩展，基于 docs/exec-plans/active/r017-patch-note-taxonomy-api.md 将 `PATCH /notes/{note_ref}` 正向升级为统一编辑入口，content 保持严格必选并继续写 revision，field 支持未传不修改、字符串设置、null 归入默认 `inbox`，tags 使用整体替换语义，返回体保持 `NoteRecord`。
- [r018] (working tree) : 2026.05.21 完成 Daily Note Counts API 开发，新增 `GET /notes/stats/daily-counts`，按服务器本地日期返回过去 30 天未删除、未归档笔记每日创建数，空日期补 0；同步 OpenAPI、自动化测试、需求澄清、设计文档和执行计划；已通过 cargo fmt --check、cargo check、cargo test 和 cargo clippy -- -D warnings 验证。
- [r021] (93d1d30) : 2026.05.22 推进代码结构重构，基于 docs/exec-plans/active/r021-code-structure-refactor.md 完成路由集成测试拆分，将 health、CORS、OpenAPI、notes、sync 相关 HTTP 行为测试迁移到 `tests/`，让 `src/app.rs` 回归路由构建职责；同时将 notes 仓储转为目录模块并拆出仓储单元测试，后续继续拆分 notes、sync 生产代码边界。
- [r022] (a8fdd4c, ac92fcb, 5edf81e) : 2026.05.23 完成并验收通过仓储结构重构，执行计划已归档到 docs/exec-plans/completed/r022-repository-structure-refactor.md；notes 仓储拆分为 core、revisions、tags、links、payloads、validation、types、tests、ids 模块，sync 仓储拆分为 mod、types、state、outbox、apply、payload、tests、ids 模块；remote apply 已引入 typed payload 与 entity/operation enum 分发，notes/sync 私有 helper 已试点领域 ID newtype；已通过 cargo fmt --check、cargo test repositories::notes、cargo test repositories::sync 和 cargo test --all-targets 验证。
- [r028] (a7c3c02) : 2026.06.19 完成 Supabase 真实同步第一验收，需求从旧的 `sync_changes` 游标投递和日志投影彻底改为九张同步表的真实双向同步：先确认本地 SQLite 与远端 Supabase/Postgres 处在同一个 `zembra-schema` contract version，再读取 `workspaces`、`devices`、`fields`、`tags`、`notes`、`note_revisions`、`note_tags`、`note_links`、`sync_changes` 两端完整快照，按主键或复合键比较差异并写回缺失或较旧的一端。真实验收脚本 `scripts/verify_supabase_real_sync.sh` 已在新 Supabase project 上验证本地已有数据严格同步：本地和远端 contract 均为 `0.5.0`，九张表完整行数据同步前后全部一致，`/sync/run` 返回 `{"pulled":0,"pushed":0}`。这次开发的主要教训是同步需求必须以真实远端数据为验收标准，不能用单元测试、mock、随机数据、接口成功或只看 `sync_changes` 代替；schema 一致性是前置契约，不能按缺表局部补丁处理；后端不能让用户拼接复杂连接串或暴露无意义 migration 开关；所有 Supabase CLI 命令必须先按本机 CLI 版本验证，禁止凭印象给不存在的命令；远端 `jsonb` 与本地 SQLite JSON text 的类型差异必须在边界层显式转换。
- [r029] (7df91df) : 2026.06.25 完成 `/health` 版本追踪机制，版本号继续以 `Cargo.toml package.version` 作为唯一来源，版本策略和发布通道闭环记录在 `package.metadata.zembra`，运行时通过新增 `src/version.rs` 解析仓库 TOML 并把 `version`、`version_policy`、`release_channel` 暴露到 `HealthResponse` 和 OpenAPI schema。本次不新增 `/version` endpoint，不使用 `.zembra.env`、CI env 或运行时外部环境变量控制版本；同时补充 r029 需求澄清、设计文档、执行计划、health 响应测试和 OpenAPI 回归测试。
- [r030] (af7a555) : 2026.06.25 完成用户级 daemon/service 初始化能力，新增 `zembra-backend init service`，保留无参数启动 HTTP server；Ubuntu 使用 `systemd --user` 和 XDG 用户目录生成 service unit、数据目录、日志目录与 `~/.zembra.env`，macOS 只初始化当前用户配置和目录，不直接调用 Homebrew service。服务启动接入 Ctrl-C 和 SIGTERM graceful shutdown，发布文档补充 Homebrew service 与 systemd user service 管理方式，本轮不创建系统用户、不要求 root、不启用未登录自动启动。
